export const JWT = {
  SECRET: 'your-secret-key',
  EXPIRES_IN: '1d',
};

export const DATABASE = {
  DEFAULT_PORT: 5432,
  DEFAULT_HOST: 'localhost',
  DEFAULT_USERNAME: 'postgres',
  DEFAULT_PASSWORD: 'postgres',
  DEFAULT_DATABASE: 'nest_blog',
};

export const SERVER = {
  DEFAULT_PORT: 3000,
  DEFAULT_ENV: 'development',
};

export const SALT_ROUNDS = 10;
